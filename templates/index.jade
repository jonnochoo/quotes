.small-12.columns
  include refreshButton
  include spinner
  ul.quotes
    li(ng-repeat='quote in quotes',ng-click="changeView('/quote/edit/' + quote._id)").quote.animate
      p {{quote.text}} - {{quote.author}} {{quote.source}}
      span(ng-repeat='tag in quote.tags').label.label-info {{tag}}